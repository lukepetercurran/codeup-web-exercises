// log out fizz if x % 3 buzz %5, fizzbuzz if both, just number if neither


for (i < 16, i = 0, i++){
    if (x/3 === 0 && x/5 === 0)
        console.log("fizzbuzz")
    if (x/3 === 0)
        console.log("fizz")
    if (x/5 === 0)
        console.log("buzz")
    else
        console.log(0)
}

// joshua peters

// RIP